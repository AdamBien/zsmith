target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
# https://maven.apache.org/wrapper/#usage-without-binary-jar
.mvn/wrapper/maven-wrapper.jar

# Eclipse m2e generated files
# Eclipse Core
.project
# JDT-specific (Eclipse Java Development Tools)
.classpath

# benchmarkLightmetalModels keeps its result tables (benchmarks/results/*.md) but not the
# per-run stdout/stderr captures, which are large and only useful while diagnosing a run
benchmarks/results/*/
benchmarks/results
